# upload

Media panel — import and browse media:
- `MediaPanel.tsx` — the media library panel.
- `MediaItem.tsx` — one media item.
- `MediaPreview.tsx` — hover/selected preview of a media item.
- `mediaImport.ts` — importing/adding media.
- `upload.css` — styles.