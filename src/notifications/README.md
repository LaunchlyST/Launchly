# notifications

Toast notifications:
- `NotificationProvider.tsx` — context provider.
- `Toast.tsx` / `NotificationToast.tsx` — toast UI.
- `NotificationContainer.tsx` — where toasts are stacked.
- `notification.types.ts` — notification types.
- `notifications.css` — styles.