# export

The export flow:
- `ExportModal.tsx` — export settings (format/resolution/quality) and the 0-100% progress bar.
- `ExportSuccess.tsx` — green success box that teleports to the empty white page after loading 0-100%.
- `export.css` — styles for the modal, success box, loading bar and white page.