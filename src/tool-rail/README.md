# tool-rail

Left tool rail — editing tools:
- `ToolRail.tsx` — the vertical rail of tool buttons.
- `ToolPanel.tsx` / `ToolsPanel.tsx` — the panel that opens for the active tool.
- `tool-rail.css` — styles.