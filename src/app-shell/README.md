# app-shell

The app entry point and main editor layout:
- `main.tsx` — React entry point.
- `Editor.tsx` — the main editor screen (wires together toolbar, preview, timeline, AI chat, export flow).
- `EditorLayout.tsx` — overall layout structure.
- `app-shell.css` — styles.