# subscription

Subscription — plans and billing state:
- `Subscription.tsx` — the subscription UI.
- `useSubscription.ts` — hook that tracks the user's plan/entitlements.
- `subscription.types.ts` — subscription/plan types.
- `subscription.css` — styles.