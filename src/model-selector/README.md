# model-selector

AI model picker — choose which model the AI assistant uses:
- `ModelSelector.tsx`, `ModelPicker.tsx`, `ModelCurve.tsx` — the picker UI.
- `aiModels.ts` — the list of available AI models.
- `model-selector.css` — styles.