# AI train system
Local AI training/running system for Launchly — not external model APIs.

- tools/   — helpers and utilities
- models/  — model configs and related assets
- train/   — training pipelines and run artifacts pointers

The UI page is at src/pages/own-train-model/
